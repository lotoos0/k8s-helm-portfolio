# Tests
pytest>=8.0
pytest-cov>=5.0

# Client HTTP for FastAPI tests ( via TestClient/AsyncClient) 
httpx>=0.27 

# LintClient HTTP for FastAPI tests ( via TestClient/AsyncClient)
 
ruff>=0.5
black>=24.8
