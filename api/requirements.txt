# Runtime dependencies
fastapi>=0.110
prometheus-client>=0.20
pydantic~=2.12 # Validation (FastAPI)

# Uvicorn with production-only extras (no watchfiles for --reload)
uvicorn>=0.23
uvloop>=0.17  # Event loop performance boost (Linux/Mac)
httptools>=0.6  # HTTP protocol performance
